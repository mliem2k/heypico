# Google Maps API Key
# Get yours from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project
# 2. Enable: Maps JavaScript API, Places API (New), Directions API
# 3. Create API key with restrictions to these APIs only
GOOGLE_MAPS_API_KEY=your_api_key_here

# Server configuration
PORT=3001
NODE_ENV=development

# CORS allowed origins (comma-separated)
# For local development with Open WebUI default:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate limiting (requests per 15-minute window)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
